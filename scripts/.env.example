################################################################################
# FICHIER D'EXEMPLE - NE CONTIENT AUCUN MOT DE PASSE RÉEL
# Copiez ce fichier vers .env et modifiez les valeurs
#
# IMPORTANT: .env est dans .gitignore et ne sera JAMAIS versionné
################################################################################

# === MOTS DE PASSE À CONFIGURER ===
# Utilisez des mots de passe forts pour la production !
# Pour générer un mot de passe fort: openssl rand -base64 16

# Mot de passe VRRP pour keepalived (8 caractères max recommandé)
VRRP_PASSWORD="CHANGEME"

# Mot de passe Rancher admin (minimum 12 caractères)
RANCHER_PASSWORD="CHANGEME"

# Mot de passe Grafana admin (minimum 12 caractères)
GRAFANA_PASSWORD="CHANGEME"

# === CONFIGURATION SSH (Optionnel) ===
# Utilisateur SSH pour déploiement sur les nœuds
SSH_USER="ubuntu"

# === NOTIFICATIONS (Optionnel) ===
# Email pour les notifications d'erreur
# NOTIFICATION_EMAIL="admin@example.com"

# Webhook Slack pour les notifications
# SLACK_WEBHOOK_URL="https://hooks.slack.com/services/YOUR/WEBHOOK/URL"

# Webhook custom pour les notifications
# CUSTOM_WEBHOOK_URL="https://your-webhook-endpoint.com/notify"
